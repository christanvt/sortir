<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="/">
			<img src="{{ asset('build/img/favicon.png') }}" alt="" width="30" height="24" class="d-inline-block align-text-top"/>
			Sortie
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link disabled">(menu de dev évolutif)</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" title="Tout les participants" href="{{ path('participant_index') }}">Participants</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" title="Tout les sorties" href="{{ path('sortie_create') }}">Créer</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" title="Tout les sorties" href="{{ path('sortie_list') }}">Sorties</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" title="Tout les campus" href="{{ path('campus_index') }}">Campus</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" title="Tout les villes" href="{{ path('ville_index') }}">Villes</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" title="Tout les lieux" href="{{ path('lieu_index') }}">Lieux</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" title="Tout les états" href="{{ path('etat_index') }}">Etats</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link disabled" title="Login" href="{{ path('app_logout') }}" style="color:red;">Connecté en tant que
							{{app.user.pseudo}}</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" title="Login" href="{{ path('app_logout') }}">Se déconnecter</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" title="Login" href="{{ path('app_login') }}">Login</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
